# Go
export GOPATH="/Users/bryce/projects/go"
export PATH=$PATH:/Users/bryce/projects/go/bin

# fnm
eval "$(fnm env --use-on-cd)"

# alias play="sudo cp -rf /etc/hosts.bryce.play /etc/hosts"
# alias work="sudo cp -rf /etc/hosts.bryce.work /etc/hosts"
# alias focus="sudo cp -rf /etc/hosts.bryce.focus /etc/hosts"

# Docker desktop
# source /Users/bryce/.docker/init-zsh.sh || true # Added by Docker Desktop

# pyenv
# export PYENV_ROOT="$HOME/.pyenv"
# command -v pyenv >/dev/null || export PATH="$PYENV_ROOT/bin:$PATH"
# eval "$(pyenv init -)"
